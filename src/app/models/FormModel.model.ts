    export class FormModel {
        // enable: boolean;
        // firstName: string;
        // lastName: string;
        constructor (public enable: boolean, public firstName: string, public lastName: string) {}
    }